# Railway will inject DATABASE_URL automatically in production
DATABASE_URL=
# Some managed Postgres require SSL
PGSSL=true
# Optional: CORS allowed origin (defaults to *)
CORS_ORIGIN=
# Port for local dev
PORT=8080
