# Railway will inject DATABASE_URL automatically in production
DATABASE_URL=
# Some managed Postgres require SSL
PGSSL=true
# Optional: CORS allowed origin list (comma separated, defaults to *)
# Example for production + local dev:
# CORS_ORIGIN=https://jettransp.site,http://localhost:8080
CORS_ORIGIN=
# Port for local dev
PORT=8080
